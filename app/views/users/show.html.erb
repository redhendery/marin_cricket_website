<% provide(:title, @user.first_name + ' ' + @user.last_name) %>

<% if @user == current_user %>
  <%= render 'users/partials/user_buttons' unless admin_logged_in? %>
<% end %>

<%= render 'users/partials/admin_buttons', user: @user if admin_logged_in? %>

<div class="columns is-vcentered">
  <div class="column">
    <h2 class="title is-2">
      <%= @user.first_name %> <%= @user.last_name %>
    </h2>
    <%= render 'shared/user_email' if admin_logged_in? %>
    <p>
      <span class="has-text-weight-semibold">
        Role:
      </span>
      <%= @user.role %>
    </p>
    <p>
      <span class="has-text-weight-semibold">
        Bats:
      </span>
      <%= @user.bats %>
    </p>
    <p>
      <span class="has-text-weight-semibold">
        Bowls:
      </span>
      <%= @user.bowls %>
    </p>
    <p>
      <span class="has-text-weight-semibold">
        About:
      </span>
      <%= @user.profile %>
    </p>
  </div>
</div>
